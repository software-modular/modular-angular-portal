export interface NavbarOption {
  name?: string,
  cssClass?: string,
  cssStyle?: string,
  redirect?: boolean,
  urlRedirect?: string,
  tooltiptext?: string,
  showOption?: boolean,
  disableOption?: boolean,
  type?: string,
  icon?: string
}
